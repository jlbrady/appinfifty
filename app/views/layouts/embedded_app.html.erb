<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
    <head>
        <title>Test Layout</title>
        <style type="text/css">
            body, html
            {
                margin: 0; padding: 0; height: 100%; overflow: hidden;
            }

            #content
            {
                position:absolute; left: 0; right: 0; bottom: 0; top: 0px;
            }
        </style>
    </head>

  <body>
    <%= render 'layouts/flash_messages' %>

    <script src="https://cdn.shopify.com/s/assets/external/app.js?<%= Time.now.strftime('%Y%m%d%H') %>"></script>

    <script type="text/javascript">
      ShopifyApp.init({
        apiKey: "<%= ShopifyApp.configuration.api_key %>",
        shopOrigin: "<%= "https://#{ @shop_session.url }" if @shop_session %>",
        debug: true,
        forceRedirect: true
      });
    </script>
    <script type="text/javascript">
        ShopifyApp.ready(function(){
            ShopifyApp.Bar.initialize({});
        });
        console.log("<%= "#{ @shop_session.url }" if @shop_session %>");
        <!--
        shop.current is the shop object, can get url from here then need to search db for
        access token and then can make api calls
        Can also use this shop
        object to get token and then store all relevant shop info and the token in pontiacDB
        along with username/pswrd by autofilling sign up form maybe -- might not satisfy shopify terms though
         -->
        console.log("<%= ShopifyAPI::Shop.current.url %>");
        console.log("<%= ShopifyAPI::AccessToken.delegate(['read_orders', 'read_products']) %>");

    </script>
    <div id="content">
      <iframe width="100%" height="100%" frameborder="0" src="https://pontiac.media/#/"/>
    </div>

    <% if content_for?(:javascript) %>
      <div id="ContentForJavascript" data-turbolinks-temporary>
        <%= yield :javascript %>
      </div>
    <% end %>
  </body>
</html>
